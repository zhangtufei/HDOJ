#include <iostream>
#include <string>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
int Calculate(const int numerator, const int denominator, char *decimaltostring)
{

	return 0;
}
int main()
{
	int a, b;
	scanf("%d/%d", &a, &b);
	char re[110];
	char s[10]; itoa(111,s,10);
	Calculate(a, b, re);
	return 0;
}